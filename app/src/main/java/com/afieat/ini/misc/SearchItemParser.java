package com.afieat.ini.misc;

/**
 * Created by Harsh Rastogi on 22/01/18.
 */

public class SearchItemParser {
   /* public Search parse(JSONObject hit) {

        if (hit==null)
        return null;
        int merchant_id = hit.optInt("merchant_id");
        String status = hit.optString("status");
        String restaurant_name = hit.optString("restaurant_name");
        String restaurant_name_ar = hit.optString("restaurant_name_ar");
        String item_id = hit.optString("item_id");
        String item_name = hit.optString("item_name");
        String item_name_ar = hit.optString("item_name_ar");
        String cuisine_id = hit.optString("cuisine_id");
        String cuisine_name = hit.optString("cuisine_name");
        String cuisine_name_ar = hit.optString("cuisine_name_ar");

        if (merchant_id != 0 &&
                status != null &&
                restaurant_name != null &&
                restaurant_name_ar != null &&
                item_id != null &&
                item_name != null &&
                item_name_ar != null &&
                cuisine_id != null &&
                cuisine_name != null &&
                cuisine_name_ar != null

                )
            return new Search(merchant_id,
                    status,
                    restaurant_name,
                    restaurant_name_ar,
                    item_id,
                    item_name,
                    item_name_ar,
                    cuisine_id,
                    cuisine_name,
                    cuisine_name_ar

                    );
        return null;

    }*/
}
